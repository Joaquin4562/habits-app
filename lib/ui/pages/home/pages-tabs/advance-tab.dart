import 'package:flutter/material.dart';

class AdvanceTab extends StatelessWidget {
  const AdvanceTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Container(
        child: Text('Advance tab'),
      ),
    );
  }
}